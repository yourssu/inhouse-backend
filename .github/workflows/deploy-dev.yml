name: Deploy to Dev

on:
  push:
    branches: [ develop ]

env:
  AWS_REGION: us-east-1
  ECR_REGISTRY: public.ecr.aws
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: dev
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR Public
        run: |
          aws ecr-public get-login-password --region us-east-1 \
            | docker login --username AWS --password-stdin public.ecr.aws

      - name: Build and push Docker image
        env:
          IMAGE_URI: public.ecr.aws/${{ secrets.ECR_PUBLIC_ALIAS }}/${{ secrets.ECR_REPOSITORY }}
        run: |
          docker build -t $IMAGE_URI:$IMAGE_TAG .
          docker tag $IMAGE_URI:$IMAGE_TAG $IMAGE_URI:dev-latest
          docker push $IMAGE_URI:$IMAGE_TAG
          docker push $IMAGE_URI:dev-latest

      - name: Deploy to Dev EC2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            IMAGE_URI="public.ecr.aws/${{ secrets.ECR_PUBLIC_ALIAS }}/${{ secrets.ECR_REPOSITORY }}"

            docker pull $IMAGE_URI:dev-latest

            docker stop inhouse-dev 2>/dev/null || true
            docker rm inhouse-dev 2>/dev/null || true

            docker run -d \
              --name inhouse-dev \
              --restart unless-stopped \
              -p 8080:8080 \
              --env-file /opt/inhouse/.env \
              -e PROFILE=dev \
              $IMAGE_URI:dev-latest

            docker image prune -f
